{
  "name": "github-actions-sdlc-demo",
  "version": "1.0.0",
  "description": "Demo application for GitHub Actions SDLC workflow",
  "main": "dist/index.js",
  "scripts": {
    "build": "node build.js",
    "test": "jest --coverage",
    "test:unit": "jest --testPathPattern=tests/unit --coverage --coverageDirectory=coverage",
    "test:integration": "jest --testPathPattern=tests/integration --runInBand",
    "lint": "eslint src/ --cache",
    "start": "node dist/index.js"
  },
  "keywords": ["github-actions", "ci-cd", "demo", "devops"],
  "author": "GitHub Actions Team",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0"
  },
  "dependencies": {
    "pg": "^8.11.3",
    "redis": "^4.6.12"
  }
}
