name: Matrix

on:
  push:
  pull_request:
  workflow_dispatch:

defaults:
  run:
    shell: bash
    working-directory: scripts

jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Hello World!'
      - run: echo $pwd
      - run: ps -p $$
    