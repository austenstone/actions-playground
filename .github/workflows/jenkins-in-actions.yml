name: Jenkinsfile in Actions

on:
  push:
    branches: [main]
    paths:
      - 'Jenkinsfile'
      - '.github/workflows/jenkins-in-actions.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  jfr:
    name: Run Jenkinsfile
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Setup Java
        uses: actions/setup-java@c5195efecf7bdfc987ee8bae7a71cb8b11521c00 # v4.7.1
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Download Jenkinsfile Runner
        run: |
          set -euo pipefail
          JFR_VERSION="1.0-beta-32"
          curl -sL "https://github.com/jenkinsci/jenkinsfile-runner/releases/download/${JFR_VERSION}/jenkinsfile-runner-${JFR_VERSION}.zip" -o jfr.zip
          unzip -q jfr.zip
          echo "=== Extracted contents ==="
          ls -la
          echo "=== Finding jenkinsfile-runner ==="
          find . -name "jenkinsfile-runner" -type f 2>/dev/null || echo "Not found"

      - name: Run Jenkinsfile
        run: |
          set -euo pipefail
          chmod +x ./jenkinsfile-runner-1.0-beta-32/bin/jenkinsfile-runner
          ./jenkinsfile-runner-1.0-beta-32/bin/jenkinsfile-runner \
            -w ./jenkinsfile-runner-1.0-beta-32/war \
            -p ./jenkinsfile-runner-1.0-beta-32/plugins \
            -f ./Jenkinsfile
