name: Retry failed jobs

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: exit $((RANDOM % 2))
      - run: gh run rerun RUN_ID --failed
